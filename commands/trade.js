const db = require("../schemas/player.js");
const Discord = require("discord.js");
const getEmoji = require('../index.js');
const itemDb = require('../schemas/items.js');

module.exports = {
  name: 'send',
  description: 'Send coins to a user.',
  category: 'handcricket',
  run: ({message}) => {
    
  }
}